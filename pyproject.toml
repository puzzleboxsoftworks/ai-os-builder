[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-os-builder"
version = "0.1.0"
description = "Describe an OS in English, get a bootable Linux ISO"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
aios = "aios.cli:main"

[tool.setuptools]
packages = ["aios"]
